FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
# Explicitly install missing dependencies
RUN npm install --save lodash react-dropzone @types/lodash @types/react-dropzone
COPY . .
EXPOSE 5173
CMD ["npm", "run", "dev"]